Test fixtures for `test_qfat.py`. Not included in this repository.

To run the tests with your own fixtures, place `.eml` files in this folder and update the `EXPECTED` dict at the top of `test_qfat.py` to map filenames to the URLs encoded in the QR codes.